package AccessmodifierA;

public class types_of_accessmodifier {
	// types of Access modifier public ,private,protected
	
	// public the access level of public modifier is everywhere it can be accessed from within the class
	//class outside the class ,within the package and outside the package 
   
	// private access level of private modifier is only within class it can not accessed outside
	// the class
	
	
	// default the access level of default modifier is only within the package 
	// it cannot accessed outside the package 
	//if do you not specify any access level it will be the default 
}
