package Variable2;

public class variablenew {// variable are nothing but piece of memory which store the value and information
// types of variable 
	// 1 local variable
	 //-non static variable
	// 2 global variable
	//-static/class global variable
	//-non static /instance global variable 
	
	// local variable a variable declared inside the body of main method 
	//syntax for  static global variable =static keyword used before the variable name in 
}     // classname.method();
// create a method out side of main method which is public static void (){}
//                    system.out.println(classname.variablename);
                    // we can also normally print by system.out.println(a);only by variable name

